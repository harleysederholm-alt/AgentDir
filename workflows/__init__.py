"""Internal cognition workflows used by ``orchestrator.WorkflowOrchestrator``.

These are *internal* to the Sovereign Engine process. They do not speak
any network protocol and should not be confused with ``a2a_protocol.py``
which handles *external* agent-to-agent messaging.
""" 
